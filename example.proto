syntax = "proto3";
package protos;

message Player{
    string name = 1;
    uint32 year = 2;
}

message PlayerReturn {
    string returnMessage = 2;
    string name = 1;
}

service PlayerService {
    rpc Example(Player) returns (PlayerReturn) {}
}